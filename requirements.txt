# requirements.txt (mit gepinnten Versionen für reproduzierbares Build)
ib_async>=1.0.0
aiosqlite>=0.20.0
aiohttp>=3.9.0
structlog>=24.0.0

# Entwicklungsabhängigkeiten
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
